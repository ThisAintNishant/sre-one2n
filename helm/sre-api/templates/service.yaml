apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre-api.fullname" . }}
  labels:
    {{- include "sre-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sre-api.selectorLabels" . | nindent 4 }}
