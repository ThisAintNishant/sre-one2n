apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sre-api.fullname" . }}-test-connection"
  labels:
    {{- include "sre-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sre-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
